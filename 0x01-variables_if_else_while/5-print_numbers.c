#include <stdio.h>

/**
 * main -> program that prints all single digit number of base 10
 * All coe should be in the main function
 * Return: 0
 */

int main(void)
{
int number;
while (number < 10)
{
printf("%d", number);
number++;
}
printf("\n");
return (0);
}
